<Page x:Class="Library.Pages.ReadersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="420" d:DesignWidth="800"
      Title="ReadersPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TBoxSearch" HorizontalAlignment="Left" Padding="5, 5, 0, 0" Width="200" Height="20" Margin="5" TextChanged="TBoxSearchTextChanged"></TextBox>

        <DataGrid x:Name="dGridReaders" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Фамилия}"></DataGridTextColumn>
                <DataGridTextColumn Header="Имя" Binding="{Binding Имя}"></DataGridTextColumn>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Отчество}"></DataGridTextColumn>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Телефон}"></DataGridTextColumn>
                <DataGridTextColumn Header="Паспорт" Binding="{Binding Паспорт}"></DataGridTextColumn>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Адрес}"></DataGridTextColumn>
                <DataGridTemplateColumn Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="BtnEdit" Content="Редактировать" Height="25" Width="120" Style="{DynamicResource ButtonStyle}" Click="BtnEditClick"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="AddEmployees" Grid.Row="2" HorizontalAlignment="Left" Content="Добавить" Height="25" Width="100" Style="{DynamicResource ButtonStyle}" Margin="10, 0, 0, 0" Click="AddReadersClick"></Button>
        <Button x:Name="DeleteEmployees" Grid.Row="2" HorizontalAlignment="Right" Content="Удалить" Height="25" Width="100" Style="{DynamicResource ButtonStyle}" Margin="0, 0, 10, 0" Click="DeleteReadersClick"></Button>
    </Grid>
</Page>
