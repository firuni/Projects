<Page x:Class="Library.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="420" d:DesignWidth="800"
      Title="EmployeesPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TBoxSearch" HorizontalAlignment="Left" Padding="5, 5, 0, 0" Width="200" Height="20" Margin="5" TextChanged="TBoxSearch_TextChanged"></TextBox>

        <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5">
            <Label Content="Фильтрация по типу:"></Label>
            <ComboBox x:Name="CBoxSortEmployees" Width="135" Padding="10, 6, 0, 0" DisplayMemberPath="Наименование" SelectionChanged="CBoxSortEmployeesSelectionChanged"></ComboBox>
        </WrapPanel>
        
        <ListView x:Name="employeesLV" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Center">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="20" Width="380">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"></RowDefinition>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        
                        <Image Width="200" Grid.Row="2" Stretch="Uniform" HorizontalAlignment="Center" Margin="5">
                            <Image.Source>
                                <Binding Path="Изображение">
                                    <Binding.TargetNullValue>
                                        <ImageSource>/Resources/picture.png</ImageSource>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>

                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Margin="5" FontSize="24">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} {1} {2}">
                                    <Binding Path="Фамилия"></Binding>
                                    <Binding Path="Имя"></Binding>
                                    <Binding Path="Отчество"></Binding>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>

                        <TextBlock Text="{Binding Position.Наименование, StringFormat={} Должность: {0}}" VerticalAlignment="Center" TextAlignment="Center"
                                   HorizontalAlignment="Center" TextWrapping="Wrap" Margin="5" FontSize="14" Grid.Row="1"></TextBlock>
                        <TextBlock Text="{Binding Телефон, StringFormat={} Телефон: {0}}" VerticalAlignment="Center" TextAlignment="Center"
                                   HorizontalAlignment="Center" TextWrapping="Wrap" Margin="5" FontSize="14" Grid.Row="3"></TextBlock>

                        <Button Name="BtnEdit" Content="Редактировать" Height="25" Width="150" Grid.Row="4" Margin="5, 15, 5, 15" HorizontalAlignment="Center" Click="BtnEditClick"></Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button x:Name="AddEmployees" Grid.Row="2" HorizontalAlignment="Left" Content="Добавить" Height="25" Width="100" Style="{DynamicResource ButtonStyle}" Margin="10, 0, 0, 0" Click="AddEmployeesClick"></Button>
        <Button x:Name="DeleteEmployees" Grid.Row="2" HorizontalAlignment="Right" Content="Удалить" Height="25" Width="100" Style="{DynamicResource ButtonStyle}" Margin="0, 0, 10, 0" Click="DeleteEmployeesClick"></Button>
    </Grid>
</Page>
